import mysql.connector
database=mysql.connector.connect(
        host='127.0.0.1',
        user= 'root',
        passwd= 'admin',
        
)
cursorObject=database.cursor()
cursorObject.execute("CREATE DATABASE hotelbookingdb")
print("all done")